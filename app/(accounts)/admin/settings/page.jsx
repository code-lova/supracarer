import React from "react";
import Settings from "@components/accounts/admin/Settings";

export const metadata = {
  title: "Settings - Supracarer",
  description: "Admin settings - Supracarer",
};

const page = () => {
  return <Settings />;
};

export default page;

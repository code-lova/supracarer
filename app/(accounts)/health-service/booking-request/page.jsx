import React from "react";
import BookingRequest from "@components/accounts/healthservice/BookingRequest";

export const metadata = {
  title: "Booking Request - Supracarer",
  description: "Health Service booking request - Supracarer",
};

const page = () => {
  return <BookingRequest />;
};

export default page;
